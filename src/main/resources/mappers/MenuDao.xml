<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tao.hai.dao.MenuDao">
    <resultMap id="BaseResultMap" type="com.tao.hai.bean.Menu">
        <id column="menu_id" property="menuId" jdbcType="VARCHAR"/>
        <result column="parent_id" property="parentId" jdbcType="VARCHAR"/>
        <result column="menu_name" property="menuName" jdbcType="VARCHAR"/>
        <result column="menu_url" property="menuUrl" jdbcType="VARCHAR"/>
        <result column="menu_Type" property="menuType" jdbcType="VARCHAR"/>
        <result column="is_Show" property="isShow" jdbcType="INTEGER"/>
        <result column="menu_sort" property="menuSort" jdbcType="INTEGER"/>
        <result column="menu_icon" property="menuIcon" jdbcType="VARCHAR"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP"/>
        <result column="del_flag" property="delFlag" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="sqlColumns">
		menu_id, parent_id, menu_name, menu_url, menu_Type,is_Show,menu_sort,menu_icon,create_date,update_date,del_flag
	</sql>
    <!--获取父节点信息-->
    <select id="getUserMenu" resultMap="BaseResultMap" parameterType="java.lang.String">
        select
        <include refid="sqlColumns"></include>
        from sys_menu m
        where m.menu_id in(
        select distinct rm.menu_id from sys_role_menu rm
        left join sys_role r on rm.role_id=R.ROLE_ID
        left join sys_user u on R.ROLE_ID=U.USER_ID
        where u.user_id=#{userId}
        )
        order by m.menu_sort,m.parent_id
    </select>
</mapper>